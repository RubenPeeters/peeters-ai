---
import Base from '~/layouts/Base.astro';
import HomeGraph from '~/components/HomeGraph.astro';

const locale = 'en' as const;
---
<Base locale={locale} routeKey="home">
  <HomeGraph locale={locale} />
</Base>
